@model IEnumerable<Category>
<div class="list-group">
    @foreach (var category in Model)
    {
        <a href="/Home/List?Category=@category.Name" class="list-group-item list-group-item-action PLcategories" style="text-transform:capitalize;">@category.Name</a>
    }
</div>
